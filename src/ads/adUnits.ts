import { TestIds } from 'react-native-google-mobile-ads';

/**
 * Placeholder ad unit IDs. These are Google's official test ad unit IDs,
 * safe to ship while the real AdMob account/app is not yet linked.
 * Replace with production ad unit IDs once the AdMob account is set up.
 */
export const BANNER_AD_UNIT_ID = TestIds.BANNER;
export const INTERSTITIAL_AD_UNIT_ID = TestIds.INTERSTITIAL;
